# Data outputs — the lake is generated by running the pipeline, never committed
lake/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Local env/config — no secrets in git (this API needs none)
.env
*.env

# Spark local-mode droppings
metastore_db/
derby.log
spark-warehouse/
